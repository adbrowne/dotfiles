- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.spacemacs: spacemacs
    ~/.inputrc: inputrc
    ~/.tmux.conf: tmux.conf

- shell:
  - [git submodule update --init --recursive, Installing submodules]
